# Used by shim/claude when it auto-starts a tmux session on the user's
# behalf. Keeps the wrapping invisible: no status bar, so a Dockling-wrapped
# session looks identical to a plain terminal running claude directly.
set -g status off
