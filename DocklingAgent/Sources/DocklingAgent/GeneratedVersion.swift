// Overwritten by tools/notarize_release.sh for a real release build, then
// restored to this default afterward — see that script for why. Never edit
// this file's value directly; it isn't meant to hold one outside a release
// build in progress.
let docklingVersion: String? = nil
